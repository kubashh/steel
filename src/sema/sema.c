#include "../lib/lib.c"

// void check(FileTokens ftokens) { // AST
//     Token tok;
//     Token prev;
//     u32 i = 0;
//     for(; (tok = ftokens.tokens[i]).type != TEOF; i++) {
//         printf("%s", tok.value);
//         prev = tok;
//     }
// }
